from core.database import Database

db = Database("db.json", password="1234")

db.schema.define({
    "id": {"type": int, "required": True},
    "name": {"type": str, "required": True},
    "email": {"type": str, "required": False, "default": "N/A"}
})

#db.insert({"id": 1, "name": "Alice"})
#db.insert({"id": 3, "name": "Lucky", "email": "lucky@example.com"})

#print(db.find({"id": 1}))


print("Min id:", db.min("id"))
print("Max id:", db.max("id"))
print("Avg age:", db.avg("id"))
print("Count users:", db.count())
print("Between age 20-40:", db.between("id", 1, 3))
print("Group by city:", db.group_by("city"))