name: ⚡ Auto Update Contributors Leaderboard

on:
  schedule:
    - cron: "0 0 * * 0"   # every Sunday at midnight UTC
  workflow_dispatch:       # manual trigger button

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Generate Leaderboard
        uses: kiranparajuli589/contributors-readme-action@v2.3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          README_PATH: README.md
          CONTRIBUTORS: 10   # top 10 contributors
          COLUMN_PER_ROW: 5  # how many per row

      - name: Commit & Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          git commit -m "⚡ Auto-update contributors leaderboard"
          git push
